spring:
  application:
    name: cardex-api

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  flyway:
    baseline-on-migrate: true
    baseline-version: 1

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

pokemon-tcg:
  base-url: https://api.pokemontcg.io/v2
  api-key: ${POKEMON_TCG_API_KEY}

exchange-rate:
  base-url: https://economia.awesomeapi.com.br
  cache-minutes: 360

cardex:
  security:
    jwt:
      secret: carddex-super-secret-development-key-2026
      expiration-minutes: 120
